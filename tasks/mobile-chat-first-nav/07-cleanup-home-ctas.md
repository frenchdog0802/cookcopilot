# 07 — Cleanup Home and CTAs

Delete `HomeScreen`. Remove `AskAiEmptyCta` from tools. Fix card navigations to drawer route names. Delete unused `AskAiEmptyCta` / `tabBarStyle` if orphaned.

**Acceptance:** No Home; no tool→AI CTAs; cards open Inventory/Shopping/etc.
