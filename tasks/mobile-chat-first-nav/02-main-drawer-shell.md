# 02 — MainDrawer shell

Replace `MainTabs` / bottom tabs in `App.tsx` with `createDrawerNavigator`. Default route `Chat`. Lazy screens. Remove stack-level duplicate AI/Subscription if they become drawer routes.

**Acceptance:** Login opens Chat; no bottom tab bar.
