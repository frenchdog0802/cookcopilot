# 04 — AppHeader hamburger

Add `showMenuButton` → `DrawerActions.openDrawer()`. Wire Chat + tool screens (no back-as-exit for root drawer screens).

**Acceptance:** Menu opens drawer on all main screens.
